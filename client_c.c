#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

#define PORT_NUM 8765

int main(int argc, char * argv[]) {

	/*
	 *	- Open up file
	 *	- Read commands from test file into an array
	 *	- Open up a socket to the server
	 * 	- Iterate through the array and pass commands to the server
	 *
	 * */
}
